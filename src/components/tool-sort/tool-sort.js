import React, {Component} from 'react';
import './tool-sort';

export default class ToolSort extends Component{
    render(){
        return(
            <a class="nav-link" href="#">Sort</a>
        );
    }
}

